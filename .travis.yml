language: sh

env:
  global:
    - CMAKE_OPTIONS=""
    - GTEST_FILTER=-cpplocate_test.getBundlePath_Return:liblocate_test.getBundlePath_Return:utils_test.getEnv_Value

matrix:
  include:
    - os: windows
      env:
        - OPTION_CMAKE_GENERATOR="Visual Studio 15 2017 Win64"
        - BUILD_DIR=build-debug
        - CMAKE_CONFIGURATION=debug
    - os: windows
      env:
        - OPTION_CMAKE_GENERATOR="Visual Studio 15 2017 Win64"
        - BUILD_DIR=build
        - CMAKE_CONFIGURATION=release

before_script:
  - eval "export ${MATRIX_EVAL}"
  - if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then export PATH="$PATH:/c/Program Files/CMake/bin"; fi
  - cmake --version
  - cmake --help
  - chmod +x ./configure
  - ./configure
  - ./configure $CMAKE_CONFIGURATION

script:
  - cmake --build $BUILD_DIR
  - cmake --build $BUILD_DIR --target test
  - cmake --build $BUILD_DIR --target check-all
